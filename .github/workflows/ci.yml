name: CI

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"

jobs:
  check:
    name: Check & Clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/checkout@v5
        with:
          repository: keyva-io/commons
          token: ${{ secrets.COMMONS_TOKEN || secrets.GITHUB_TOKEN }}
          path: commons
      - run: ln -s $GITHUB_WORKSPACE/commons ../commons
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo clippy --workspace --all-targets -- -D warnings

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/checkout@v5
        with:
          repository: keyva-io/commons
          token: ${{ secrets.COMMONS_TOKEN || secrets.GITHUB_TOKEN }}
          path: commons
      - run: ln -s $GITHUB_WORKSPACE/commons ../commons
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo test --workspace

  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/checkout@v5
        with:
          repository: keyva-io/commons
          token: ${{ secrets.COMMONS_TOKEN || secrets.GITHUB_TOKEN }}
          path: commons
      - run: ln -s $GITHUB_WORKSPACE/commons ../commons
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all -- --check

  deny:
    name: Dependency Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/checkout@v5
        with:
          repository: keyva-io/commons
          token: ${{ secrets.COMMONS_TOKEN || secrets.GITHUB_TOKEN }}
          path: commons
      - run: ln -s $GITHUB_WORKSPACE/commons ../commons
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo install cargo-deny --quiet
      - run: cargo deny check

  release:
    name: Release (${{ matrix.target }})
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [check, test, fmt]
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-musl
            os: ubuntu-latest
          - target: x86_64-apple-darwin
            os: macos-latest
          - target: aarch64-apple-darwin
            os: macos-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/checkout@v5
        with:
          repository: keyva-io/commons
          token: ${{ secrets.COMMONS_TOKEN || secrets.GITHUB_TOKEN }}
          path: commons
      - run: ln -s $GITHUB_WORKSPACE/commons ../commons
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Install musl tools (Linux)
        if: contains(matrix.target, 'linux-musl')
        run: sudo apt-get update && sudo apt-get install -y musl-tools

      - name: Build
        run: cargo build --release --target ${{ matrix.target }} -p transit-server -p transit-cli

      - name: Package
        run: |
          mkdir -p dist
          # keyva-transit: server + cli bundled
          cp target/${{ matrix.target }}/release/keyva-transit dist/
          cp target/${{ matrix.target }}/release/keyva-transit-cli dist/
          cd dist && tar czf keyva-transit-${{ github.ref_name }}-${{ matrix.target }}.tar.gz keyva-transit keyva-transit-cli && cd ..
          rm dist/keyva-transit dist/keyva-transit-cli

      - name: Upload release artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.target }}
          path: dist/*.tar.gz

  docker:
    name: Docker (${{ matrix.image }})
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [check, test, fmt]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        include:
          - image: keyva-transit
            target: keyva-transit
          - image: keyva-transit-cli
            target: keyva-transit-cli
    steps:
      - uses: actions/checkout@v5
        with:
          path: transit
      - uses: actions/checkout@v5
        with:
          repository: keyva-io/commons
          token: ${{ secrets.COMMONS_TOKEN || secrets.GITHUB_TOKEN }}
          path: commons
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: transit/Dockerfile
          target: ${{ matrix.target }}
          push: true
          tags: |
            ghcr.io/keyva-io/${{ matrix.image }}:${{ github.ref_name }}
            ghcr.io/keyva-io/${{ matrix.image }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  publish-release:
    name: Publish GitHub Release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [release]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          pattern: release-*
          merge-multiple: true
      - uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*.tar.gz
          generate_release_notes: true
